# tuples are surrounded by parenthesis `()` as seen below

car = ("honda", "toyota", "kia", "audi")
print(car[0])

# because this is a tuple, it cannot be modified or deleted in the stack trace. 

del (car)

print(car)